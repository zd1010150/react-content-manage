import Enums from 'utils/EnumsManager';

const { VIEWDETAILS } = Enums.ReduxActionTypePrefix;

export const SET_MODULE_DATA = `${VIEWDETAILS}SET_MODULE_DATA`;
export const SET_MODULES = `${VIEWDETAILS}SET_MODULES`;
